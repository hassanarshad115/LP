<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hassan.lp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/lplogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DownloadsActivity"></activity>
        <activity android:name=".ConfirmYourDetailFolder.ConfirmYourDetialActivity" />
        <activity android:name=".AddYourIdFolder.AddYourIdActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SigninandSignupActivity" />
        <activity android:name=".DashboardActivity" />
    </application>

</manifest>